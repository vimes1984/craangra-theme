<?xml version="1.0"?>
<ruleset name="Craangra Theme Standards">
    <description>PHP CodeSniffer rules for Crann Gra WordPress theme</description>

    <!-- Files to scan -->
    <file>.</file>

    <!-- Exclude patterns -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*.min.js</exclude-pattern>
    <exclude-pattern>*.min.css</exclude-pattern>

    <!-- Use WordPress-Extra as base -->
    <rule ref="WordPress-Extra">
        <!-- Allow short ternary -->
        <exclude name="WordPress.PHP.DisallowShortTernary"/>
        <!-- Allow PHP short echo tags -->
        <exclude name="WordPress.PHP.DisallowShortOpenTag"/>
    </rule>

    <!-- Add WordPress Docs -->
    <rule ref="WordPress-Docs"/>

    <!-- Add WordPress PHP Compatibility -->
    <rule ref="PHPCompatibilityWP"/>

    <!-- Set PHP version -->
    <config name="testVersion" value="8.0-"/>
    <config name="minimum_supported_wp_version" value="6.4"/>

    <!-- Check for PSR-4 compatible namespace declarations -->
    <rule ref="Generic.NamingConventions.UpperCaseConstantName"/>
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="150"/>
        </properties>
    </rule>

    <!-- Require spaces around operators -->
    <rule ref="Generic.Formatting.SpaceAfterNot"/>
    <rule ref="Squiz.WhiteSpace.OperatorSpacing"/>
    <rule ref="Squiz.Strings.ConcatenationSpacing">
        <properties>
            <property name="spacing" value="1"/>
        </properties>
    </rule>

    <!-- No trailing whitespace -->
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace"/>

    <!-- File ending newline -->
    <rule ref="Generic.Files.EndFileNewline"/>
</ruleset>
